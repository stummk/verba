# Core dependencies — deliberately small so the first start is fast.
# Heavy feature groups (Whisper, embeddings, LLM, PDF) are installed by the
# onboarding at runtime; defined in backend/verba/setup_check.py.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
python-multipart>=0.0.9
